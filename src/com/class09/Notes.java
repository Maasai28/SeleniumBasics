package com.class09;public class Notes {
/*
 * Synchronizaton
 *  is a process of matching speed of applications with automation tools(selenuim, QTP)
 *  1.Unconditional - Thread.sleep
 *  2.Conditional - we can specify time
 *  
 *  PageLoadTomeOut
 *   - waits for all elements in DOM to get fully loaded
 *   -appilcable for page when we use .ge() & .navigate():
 * 
 * Selenium Waits:
 * 
 * Implicit wait : Global Wait - sprcified amount of time before it throws NoSuchElementException
 *                -aplicable only for -findElement
 *                                     -findElements
 *                -if we do not specify the default value is =0
 * 
 * Explicit Wait : WebDriver Wait
 *                - works only with specified element
 *                -works with some ExpectedConditions
 *       ExpectedConditions is a class with many options/methods
 *                   . elementToBeClickable
 *                   . element
 *                
 *                 Fluent Wait
 */
}
